export interface MochilaDefinida {
  id: number;          // identificador único
  patron: string;
  color: string;
  medida: string;
  precioCOP?: number;
  precioUSD?: number;
  imagen?: string; 
}
